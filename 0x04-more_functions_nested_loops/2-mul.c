#include "main.h"

/**
 * mul - function to multiply two integers
 * @a: the first integer
 * @b: the second integer
 *
 * Return: product of a * b
 */
int mul(int a, int b)
{
	int c = (a * b);

	return (c);
}
